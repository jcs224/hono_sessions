{
  "scripts": {
    "dev:cookie": "bun run --hot src/test_cookie.ts",
    "dev:sqlite": "bun run --hot src/test_sqlite.ts"
  },
  "dependencies": {
    "hono": "^3.12.8",
    "hono-sessions": "../../npm"
  },
  "devDependencies": {
    "bun-types": "^0.6.2"
  }
}
